/**
 * Capitalize a specific string
 * @param word: Any string that needs to be capitalized
 * @return {string}
 */
export function capitalize(word) {
  return word.charAt(0).toUpperCase() + word.slice(1);
}
